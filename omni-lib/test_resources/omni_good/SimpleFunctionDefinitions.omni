module SimpleFunctionDeclaration()

// Prelude isn't implemented yet, so provide dummy + 
infixl +(x: Int, y: Int): Int = 0

nullary_function(): Int = 1

oneline_function(x: Int, y: Int) : Int = x + y

wrapped_function(x: Int, y: Int) : Int =
  x + y

main() : Int =
  oneline_function(nullary_function(), 2)